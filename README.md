To build the docker container run ‘sudo docker compose build’

To run the docker container run ‘sudo docker compose run  –-rm ros2’

The program sbp_publisher.py will publish messages to ROS topics using information from the Piksi board.
